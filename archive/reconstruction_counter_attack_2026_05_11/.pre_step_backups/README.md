# Pre-Edit Backup Files -- Action MOEX Session

These are session-specific pre-edit backups created during the Action MOEX Steps 1-6 closure (2026-05-11) and the preceding Latest-Chain orchestration, retained as forensic evidence. Each backup file named `<basename>.pre_stepN_backup` or `<basename>.pre_latest_chain_stepN_backup` mirrors the production file `<basename>` at its state immediately before the corresponding step's edit was applied. Retention policy: do not modify, do not delete without explicit user approval; the active source of truth is the production file each backup mirrors.

Closing date: 2026-05-11.
